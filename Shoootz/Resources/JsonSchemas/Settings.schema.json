{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "PortNumber": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535
    }
  },
  "type": "object",
  "properties": {
    "CurrentLanguageCode": {
      "enum": ["de", "en"]
    },
    "Database": {
      "type": "object",
      "properties": {
        "ConnectionString": {
          "type": "string"
        },
        "Provider": {
          "enum": ["PostgreSql", "Sqlite"]
        }
      },
      "required": ["ConnectionString", "Provider"],
      "additionalProperties": false
    },
    "UDP": {
      "type": "object",
      "properties": {
        "AutoConnect": {
          "type": "boolean"
        },
        "Port": {
          "$ref": "#/definitions/PortNumber"
        }
      },
      "required": ["AutoConnect", "Port"],
      "additionalProperties": false
    }
  },
  "required": ["CurrentLanguageCode", "Database", "UDP"],
  "additionalProperties": false
}
